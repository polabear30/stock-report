# Cloud Run Job(일일 리포트) 전용 슬림 의존성.
# 전체 requirements.txt(streamlit/plotly/anthropic 등)는 배치에 불필요.
# build_html 경로는 yfinance+requests만, 업로드는 google-cloud-storage만 쓴다.
yfinance>=0.2.30
requests>=2.31.0
google-cloud-storage>=2.16.0
